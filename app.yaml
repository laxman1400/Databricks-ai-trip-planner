command:
  - streamlit
  - run
  - app.py
  - --server.address
  - 0.0.0.0
  - --server.port
  - ${DATABRICKS_APP_PORT}

env:
  - name: ENDPOINT_NAME
    valueFrom: postgres

  - name: STREAMLIT_GATHER_USAGE_STATS
    value: "false"
